using System.Collections;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Reflection;
using System.Security.AccessControl;
using System.Security.Cryptography;
using System.Security.Cryptography.X509Certificates;
using UnityEngine;
using UnityEngine.UI;

/*
public class Graph : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        graphContainer = transform.Find("GraphContainer").GetComponent<RectTransform>(); 
        CreateBar(new Vector2(200, 200))
    }

    private Sprite rectSprite;

    // Update is called once per frame

    //Y axis
        //graph height
    //X axis   
        //graph width



    private void CreateBar(Vector2 setPosition, int width)
    {
        GameObject gameObject = new GameObject("rect", typeof(Image)); //create rectangle
        RectTransform barTransform = gameObject.GetComponent<rectTransform>();
        barTransform.anchoredPosition = setPosition;
        barTransform.sizeDelta = new Vector2(10, 10); 
        //positon min/max? 
    }


    private void displayGraph(List<int> numInfected) 
    {
        //given the value, display it
        float yAxis = GraphContainer;
    


    }
}
*/